Extract the zip file into the web root server.

Assuming that apache server is up and running, Open the files in the browser and it should be working without any problem.

The code has been tested with IE and FF
